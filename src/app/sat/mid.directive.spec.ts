import { MidDirective } from './mid.directive';

describe('MidDirective', () => {
  it('should create an instance', () => {
    const directive = new MidDirective();
    expect(directive).toBeTruthy();
  });
});
